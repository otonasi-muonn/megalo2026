# 🚀 デプロイ設定 (シンプル版)

## 1. 環境一覧
| 環境 | ホスティング | URL | 備考 |
| --- | --- | --- | --- |
| 本番 (Production) | [例: Vercel] | `https://myapp.com` | `main` ブランチにPushで自動デプロイ |
| 開発 (Preview) | [例: Vercel] | 自動生成URL | PR作成時にプレビュー環境を構築 |

## 2. ビルド設定
* **フレームワーク**: [例: Next.js]
* **ビルドコマンド**: `npm run build`
* **出力ディレクトリ**: `.next` (または `dist` など)

## 3. インフラ上の注意点・制約
* [例: Vercelの無料枠(Hobby)のため、サーバーレス関数の実行タイムアウトは10秒以内にする]
* [例: cronジョブの設定は `vercel.json` で管理する]