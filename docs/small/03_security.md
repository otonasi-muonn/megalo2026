# 🔐 セキュリティ・権限設計

## ロール（権限レベル）
* `ADMIN`: 管理者。全データを操作可能。
* `USER`: ログインユーザー。機能を利用可能。
* `GUEST`: 未ログイン。公開ページのみ閲覧可能。

## アクセス制御ルール
* **データ作成**: `USER` 以上が必要。
* **データ編集・削除**: そのデータの**作成者本人** または `ADMIN` のみ可能。
* **データ閲覧**: 全員可能（または作成者のみ）。