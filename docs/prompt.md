現在以下のようなISSUEになっています。
重要度の高いEPICのISSUEを終わらせたいと考えています。
ブランチを私が切るので1コミット単位で出力し、ブランチ単位で開発を行ってください

[Epic]: 本番稼働準備とインフラ運用 otonasi-muonn/megalo2026#32
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: epic


  ### 背景と目的

  開発環境で完成したアプリケーションを、一般ユーザーがアクセス可能な本番環境へと移行し、安定稼働させるための基盤を構築します。
  ホスティングサービスおよびデータベース基盤を用いたCI/CD（継続的インテグレーション/継続的デプロイ）の連携により、コードの変更を自動的かつ安全に本番環境へ反映させるデプロイメントパイプラインを確立します。また、予期せぬエラーや不正 なアクセスに対する防御機構（フェイルセーフ）を実装し、プラットフォームとしての信頼性を担保することを目的とします。

  ### タスク一覧（子Issue）

  [ ] #24 本番プロジェクトの設定とCI/CD連携の構築
  [ ] #25 未認証アクセスおよびAPI通信エラー時のフォールバックUI・リダイレクト処理の実装

  ### 備考・参考資料

  デプロイ設定ドキュメント（ 05_deployment.md 
  ）。環境変数の設定ミスは重大なセキュリティインシデントに直結するため、本番用と開発用のキーの取り扱いには細心の注意を払ってください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/32

------------------------------------------

[Epic]: 公式コンテンツ制作とリリース品質保証 otonasi-muonn/megalo2026#31
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: epic


  ### 背景と目的

  システム機能が概ね完成した段階で、プロダクトを実際のゲームとして遊べる状態へ引き上げます。開発チーム自身がエディタ機能を用いて公式ステージを制作し、ゲームエンジンの最終的な品質検証（ドッグフーディング）を実施します。併せて、モバ イル端末での操作性最適化とSNS展開時の拡散力強化を行い、リリース初期のユーザー体験とトラクションの最大化を図ります。

  ### タスク一覧（子Issue）

  [ ] #21 エンジン品質検証を兼ねた、チュートリアルおよび公式サンプルステージのデータ制作
  [ ] #22 スマートフォン実機ブラウザ向けのスワイプ操作最適化（デフォルトスクロールの無効化）
  [ ] #23 SNSシェア展開時の動的OGP（メタデータ）生成機能の実装

  ### 備考・参考資料

  プロジェクト概要資料に記載されたターゲット層（ゲーム実況者とその視聴者）を意識し、初見で遊び方が直感的に伝わるコンテンツ作りと、SNS等で拡散されやすい導線設計に注力してください。背景知識として、ユーザーの定着率は初期体験に大きく依存するため、このフェーズでの品質保証がプロダクトの成否を分けます。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/31

------------------------------------------

[Epic]: 物理演算ゲームエンジンとコアロジックの完成 otonasi-muonn/megalo2026#30
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: epic


  ### 背景と目的

  本プロジェクトの中核となる、ブラウザ上で完結する物理演算アクションゲームの体験を構築します。先行フェーズで初期化したキャンバス領域において、「スワイプ操作による風の発生」という基本操作や、UI側で配置されたギミックの描画、およびデ ータ変換処理を実装します。さらに、「制作者自身がクリアできた場合のみステージの公開を許可する」というフェイルセーフ機能の要となる、ゲームのクリア判定とUIへの状態連携ロジックを完成させます。

  ### タスク一覧（子Issue）

  [ ] #18 スワイプ操作による風の発生ロジックと、物理演算パラメーターの調整
  [ ] #19 エディタ操作に基づくギミックの配置反映と、構造化データへのシリアライズ処理の実装
  [ ] #20 テストプレイにおけるクリア判定と、UI側への公開許可フラグ送信ロジックの実装

  ### 備考・参考資料

  フロントエンド・ゲーム設計仕様書。ゲームエンジン管轄内の処理となるため、フロントエンドのDOM操作を直接行わず、用意された通知処理のみで外部とやり取りする設計を徹底してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/30

------------------------------------------

[Epic]: フロントエンドUIとエディタ状態管理の実装 otonasi-muonn/megalo2026#29
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: epic


  ### 背景と目的

  先行フェーズで用意した画面の枠組みに対し、実際のUIとデータフローを構築します。
  認証基盤を用いたログイン状態のグローバルな管理と、本プロジェクトの技術的な要である「ステージ制作エディタ」における複雑な状態管理（フロントエンドとゲームエンジン間のデータ同期）の土台を完成させることを目的とします。

  ### タスク一覧（子Issue）

  [ ] #15 認証UIとセッション（ログイン状態）管理の実装
  [ ] #16 エディタにおける「配置データ」の集中状態管理と、ゲームエンジンとの同期ロジックの実装
  [ ] #17 ダッシュボード、ステージ一覧、およびリザルト画面のUIコンポーネント実装

  ### 備考・参考資料

  フロントエンド・ゲーム設計仕様書（ 03_front.md 
  ）。UIの見た目だけでなく、背後で流れるデータ（状態）の設計に注力するフェーズです。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/29

------------------------------------------

[Epic]: バックエンドロジックとデータベースの完成 otonasi-muonn/megalo2026#28
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: epic


  ### 背景と目的

  Epic 1で構築したAPIの「ガワ（空のルーティング）」に対し、実際のデータベース操作と認証による保護処理を組み込みます。

  ユーザー情報、ステージ情報、プレイ履歴などを永続化する堅牢なデータ基盤を確立し、フロントエンドから送信されるデータが安全かつ正確に保存・提供される状態を作ることを目的とします。

  ### タスク一覧（子Issue）

  [ ] #12 データベースのテーブル群（プロフィール、ステージ、プレイ履歴、いいね）のスキーマ構築
  [ ] #13 認証イベントを検知するDBトリガーおよびプロフィール自動生成機能の実装
  [ ] #14 HonoにおけるJWT検証ミドルウェアと、各種データのCRUDロジックの実装

  ### 備考・参考資料

  データベース設計仕様書（ 02_db-design.md ）、API設計仕様書（ 01_api-design.md ）。

  本Epicが完了すると、フロントエンド側は本物のデータベースと連動したUIの構築（Epic 3）に着手できるようになります。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/28

------------------------------------------

[Epic]: システムの骨格構築と疎通確認 otonasi-muonn/megalo2026#27
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: epic


  ### 背景と目的

  各担当者が他者の進捗を待たずに非同期で開発を進められるよう、システム全体の「枠組み」を先行して構築します。

  バックエンドのAPIルーティング、フロントエンドの画面遷移、およびゲームエンジンの初期化を実施し、フロントエンドからバックエンドまでモックデータがエラーなく連携・疎通する状態を確立することを目的とします。

  ### タスク一覧（子Issue）

  [ ] #9 バックエンドにおける全APIエンドポイントの空ルーティングとモックデータ返却の実装
  [ ] #10 フロントエンドにおける全画面のルーティングとAPI通信のボイラープレート実装
  [ ] #11 フロントエンドコンポーネント内へのKAPLAYキャンバスの配置と初期化処理

  ### 備考・参考資料

  本Epicでは実際のデータベース接続や複雑な状態管理は実装せず、あくまで「ガワ（枠組み）」の完成と疎通確認に特化します。

  API設計仕様書、およびフロントエンド・ゲーム設計仕様書（ 03_front.md ）を参照してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/27

------------------------------------------

[Epic]: 開発基盤の整備と共通仕様の確立 otonasi-muonn/megalo2026#26
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: epic


  ### 背景と目的

  チーム全体が同じ前提・環境で、非同期に開発を進めるための強固な土台を構築します。
  具体的には、モノレポ全体で共通利用する「データ構造（JSONスキーマ）の型定義」と、各開発者のローカルPC上で独立して動作する「DB環境（Supabaseエミュレーター）の構築」を最優先で実施します。これにより、後続の開発フェーズにおける「デー タの解釈違い」や「開発環境の競合」といった致命的な手戻りリスクを未然に防ぎます。

  ### タスク一覧（子Issue）

  [ ] #7 共有パッケージにおけるステージデータの型定義とドメインモデルの設計
  [ ] #8 Supabase CLIを用いたローカル開発用エミュレーター環境の構築

  ### 備考・参考資料

  本プロジェクトのアーキテクチャ（完全な型共有と自律したDB環境）の根幹を担う最重要フェーズです。必ずフロントエンドおよびバックエンドの各担当者間で仕様の合意形成を完了させてから、次のEpicへ移行してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/26

------------------------------------------

[Task]: 未認証アクセスおよびAPI通信エラー時のフォールバックUI・リダイレクト処理の実装 otonasi-muonn/megalo2026#25
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  ユーザーが想定外の順序で操作を行った場合や、ネットワーク切断等により通信が失敗した場合の防御機構を実装します。
  未ログイン状態のユーザーが認証必須の画面（ステージ作成やダッシュボード等）へ直接アクセスした際、強制的にログイン画面（またはホーム画面）へリダイレクトさせるルーティングの保護を構築します。また、APIからのデータ取得失敗時に画面が真っ白になる事態を防ぐため、エラーメッセージやリトライボタンを表示するフォールバックUIを実装します。

  ### 完了の定義（Doneの条件）

  [ ] 未認証状態で保護されたルート（ /create 等）へアクセスした際、適切な画面へリダイレクトされること。
  [ ] 存在しないステージIDなどの無効なURLにアクセスした際、404エラー画面の表示またはホーム画面への誘導が行われること。
  [ ]
  通信エラーやサーバーエラー（HTTPステータス500系など）が発生した際、ユーザーフレンドリーなエラーメッセージが画面上に表示され、アプリケーション全体がクラッシュしないこと。

  ### 備考・参考リンク

  ルーティングライブラリの機能（エラーバウンダリなど）を活用し、コンポーネントツリー全体をエラーから保護する設計を採用してください。


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/25

------------------------------------------

[Task]: 本番プロジェクトの設定とCI/CD連携の構築 otonasi-muonn/megalo2026#24
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  ホスティングプラットフォーム上にフロントエンドおよびバックエンド（API）をデプロイするための本番用プロジェクトを作成し、本番用のデータベース環境を立ち上げます。
  ソースコードリポジトリとホスティング環境を連携させ、メインブランチへのマージをトリガーとして自動的にビルドとデプロイが実行されるパイプラインを構築します。同時に、本番用データベースに対して開発環境で構築したスキーマとデータベース トリガーを適用します。

  ### 完了の定義（Doneの条件）

  [ ] 本番環境が稼働し、公開URLからアプリケーションにアクセスできること。
  [ ] ホスティング環境の環境変数に、本番用データベースのURLおよびAPIキーが正しく設定されていること。
  [ ] メインブランチへコードをプッシュした際、自動ビルドが実行され、変更が本番URLに遅滞なく反映されること。
  [ ] Pull Requestの作成時に、動作確認用のプレビュー環境が自動生成されること。

  ### 備考・参考リンク

  ホスティングサービスの無料枠などを利用する場合、サーバーレス関数の実行時間に制限（例：10秒）が設けられていることがあるため、制限時間内にすべてのAPI処理が完了するかのパフォーマンス検証も併せて実施してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/24

------------------------------------------

[Task]: SNSシェア展開時の動的OGP（メタデータ）生成機能の実装 otonasi-muonn/megalo2026#23
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  ユーザーが特定のステージURLをSNS等でシェアした際、リンク先のステージタイトルや固有の画像がリッチなカード形式で表示されるよう、バックエンドから画面の枠組みを返却する時点で動的にメタデータを埋め込む仕組みを構築します。

  ### 完了の定義（Doneの条件）

  [ ]
  ステージのプレイ画面URLにアクセスした際、HTMLのヘッダ領域にそのステージ固有の情報を含むメタデータが出力されていること。
  [ ]
  プログラムを実行しないSNSのデータ収集ボット（クローラー）からのアクセスに対しても、正しいメタデータが返却されること。
  [ ] 各種SNSのメタデータ検証ツールにて、情報が正しくカード表示されることが確認できていること。

  ### 備考・参考リンク

  フロントエンド側での画面描画（クライアントサイドラウティング）のみに依存すると、SNSのボットが情報を読み取れない可能性が高くなります。そのため、バックエンドAPI側での初期画面生成時にメタデータを付与する構造的な対策が必須となります 。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/23

------------------------------------------

[Task]: スマートフォン実機ブラウザ向けのスワイプ操作最適化（デフォルトスクロールの無効化） otonasi-muonn/megalo2026#22
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  ゲームのコア体験であるスワイプ操作において、スマートフォンブラウザ特有の標準挙動（画面スクロール、引っ張って更新、戻る・進むのジェスチャーなど）が誤動作を引き起こすのを防ぎます。ゲーム画面領域に対するブラウザの標準的なタッチ操作 を無効化し、純粋なゲーム内入力としてのみ受け付ける制御を実装します。

  ### 完了の定義（Doneの条件）

  [ ] 主要なモバイルOS（iOS、Android）の実機ブラウザ環境でテストプレイが実施されていること。
  [ ] 画面上で連続したスワイプ操作を行っても、画面全体がスクロールしたり揺れたりしないこと。
  [ ] スワイプ操作が、ゲームエンジン内の入力処理としてのみ正確に解釈されること。

  ### 備考・参考リンク

  スタイルシートによるタッチ操作の制御や、プログラムによるブラウザ標準イベントのキャンセル処理などの仕組みを調査し、適切に適用してください。モバイルでの操作ストレスは離脱の直接的な原因となるため、実機での入念な確認が必要です。     

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/22

------------------------------------------

[Task]: エンジン品質検証を兼ねた、チュートリアルおよび公式サンプルステージのデータ制作 otonasi-muonn/megalo2026#21
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  完成したエディタを用いて、運営提供の初期コンテンツとなる「チュートリアルステージ（基本操作の学習用）」および「サンプルステージ（物理演算の応用編）」を複数制作し、初期データとして登録します。この制作過程を通じてエディタの操作性や 物理演算の不自然な挙動を洗い出し、先行する開発フェーズへの最終的な修正フィードバックを行います。

  ### 完了の定義（Doneの条件）

  [ ] 初心者向けのチュートリアルステージが最低1つ制作され、公開状態になっていること。
  [ ] ゲームの魅力を伝える応用・サンプルステージが複数制作され、公開状態になっていること。
  [ ] 制作過程で発見された不具合やUIの改善点が、必要に応じて別のバグ修正Issueとして起票されていること。
  [ ] リリース直後、新規ユーザーのホーム画面にこれらの公式ステージが一覧表示されること。

  ### 備考・参考リンク

  コンテンツが空の状態でリリースされるのを防ぐための最重要タスクです。プレイヤーが段階的に操作を学べるよう、難易度曲線を意識した設計を行ってください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/21

------------------------------------------

[Task]: テストプレイにおけるクリア判定と、UI側への公開許可フラグ送信ロジックの実装 otonasi-muonn/megalo2026#20
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  ゲームエンジン内でキャラクターがゴールに到達したこと、または障害物に触れて失敗したことを検知する当たり判定を実装します。テストプレイにおいてゴール条件を満たした場合、フロントエンド側へ「クリア成功」の通知を送り、UI上の「公開する 」ボタンを活性化させるための状態更新処理を連携させます。

  ### 完了の定義（Doneの条件）

  [ ] ゴールオブジェクトとキャラクターの接触判定が正常に機能し、クリアを検知できること。
  [ ] 障害物への接触等によるゲームオーバー判定が正常に機能すること。
  [ ] クリア成功時、フロントエンド側の「クリア確認済み状態」が有効化されること。
  [ ]
  テストプレイ終了後、エディタ上でギミックの配置を1つでも変更した場合、即座にクリア確認済みの状態が無効化される一連のフローが動作すること。

  ### 備考・参考リンク

  フロントエンド・ゲーム設計仕様書の「状態管理とクリア制限付き公開機能のロジック」を遵守してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/20

------------------------------------------

[Task]: エディタ操作に基づくギミックの配置反映と、構造化データへのシリアライズ処理の実装 otonasi-muonn/megalo2026#19
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  フロントエンド側で管理している配置状態のデータを受け取り、対応するギミック（トゲ、ばね等）としてゲームエンジン上に描画する処理を実装します。同時に、現在のキャンバス上に存在する全オブジェクトの座標や属性を抽出し、データベース保存 用のJSON構造へ再変換（シリアライズ）する出力処理を構築します。

  ### 完了の定義（Doneの条件）

  [ ] フロントエンド側から渡された初期データを元に、ゲームエンジン上でステージの配置が正確に再現されること。
  [ ] データ出力処理を実行した際、キャンバスの現在の状態が仕様通りのJSON形式で出力されること。
  [ ] 出力されたJSONデータを用いて再読み込みを行った場合でも、ステージの配置が完全に一致すること。

  ### 備考・参考リンク

  UI操作による座標のズレを防ぐため、座標系の基準（画面中央、あるいは左上など）を明確に定義して実装を行ってください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/19

------------------------------------------

[Task]: スワイプ操作による風の発生ロジックと、物理演算パラメーターの調整 otonasi-muonn/megalo2026#18
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  ゲームエンジン内において、ユーザーのスワイプ操作（開始点と終了点）からベクトルを算出し、キャラクターに対して「風（物理的な力）」を与えるアクションロジックを実装します。あわせて、重力、キャラクターの質量、風の減衰率などの物理演算 パラメーターを調整し、ゲームとして心地よい操作感の基盤を確立します。

  ### 完了の定義（Doneの条件）

  [ ] キャンバス上のスワイプ操作に反応し、キャラクターに力が加わって移動すること。
  [ ] スワイプの距離や速度に応じて、加わる力の強さや向きが適切に変化すること。
  [ ] 画面外への落下判定など、基本的な物理法則が適用されていること。

  ### 備考・参考リンク

  キャラクターを直接引っ張るのではなく、空間に力を発生させる仕様であるため、将来的な視覚効果（風のエフェクトなど）の拡張性も考慮した設計を推奨します。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/18

------------------------------------------

[Task]: ダッシュボード、ステージ一覧、およびリザルト画面のUIコンポーネント実装 otonasi-muonn/megalo2026#17
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  用意された画面の枠組みに対して、具体的なデザインとコンポーネントを適用します。ホーム画面の「公開済みステージ一覧」、ダッシュボードの「自作ステージ管理（編集・削除アクション等）」、およびプレイ後の「結果表示（クリア/失敗、リトライ操作等）」のUIを構築します。

  ### 完了の定義（Doneの条件）

  [ ] ホーム画面において、ステージが一覧形式で表示され、プレイ画面への導線が機能していること。
  [ ] ダッシュボード画面において、ユーザー自身のステージ一覧が表示され、編集および削除アクションのUIが存在すること。
  [ ] リザルト画面において、プレイの判定結果が表示され、別画面への導線が機能していること。
  [ ] ステージのシェア導線として、 /play/[id] 
  のダイレクトリンクをクリップボードへコピーできるUIが実装されていること。
  [ ] プレイ終了時に  POST /api/stages/:id/play_logs 
  を呼び出し、結果画面表示とプレイログ記録のフローが連動していること。
  [ ] 各UIコンポーネントが、バックエンドAPIからデータを取得して動的に表示できる状態にあること。

  ### 備考・参考リンク

  一覧表示の画面では、データ取得中のローディング表現や、データが0件だった場合の空状態（Empty
  State）の表示も漏れなく実装してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/17

------------------------------------------

[Task]: エディタにおける「配置データ」の集中状態管理と、ゲームエンジンとの同期ロジックの実装 otonasi-muonn/megalo2026#16
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  ステージ制作画面において、UI（ギミックパレット）からキャンバスへ追加されたオブジェクトの「種類」「座標」「属性」を、フロントエンド側の状態として一元管理する仕組みを構築します。また、フロントエンド側で保持するデータが更新された際 、ゲームエンジン側のインスタンスへ最新の配置データを安全かつ単方向に同期するインターフェースを作成します。

  ### 完了の定義（Doneの条件）

  [ ] UI上のパレットからアイテムを選択・配置する操作により、フロントエンド側のデータ構造（Epic
  0で定義したJSON構造の配列）が正しく更新されること。
  [ ] 配置されたアイテムの移動や削除操作に対して、状態データが矛盾なく更新されること。
  [ ]
  フロントエンド側の状態が変更された際、ゲームエンジンの描画内容に遅滞なく反映される単方向の同期処理が機能していること。

  ### 備考・参考リンク

  ゲームエンジン側での操作（キャンバス内でのドラッグ等）と、UI側での操作が競合しないよう、状態更新の責任分界点を明確に設計してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/16

------------------------------------------

[Task]: 認証UIとセッション（ログイン状態）管理の実装 otonasi-muonn/megalo2026#15
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  フロントエンドアプリケーション内で認証クライアントを初期化し、ユーザーのログイン状態（セッション）を監視・管理するグローバルな仕組みを実装します。ヘッダー等にログインおよびログアウト用のボタンを配置し、認証状態に応じてUIの出し分 けを行います。

  ### 完了の定義（Doneの条件）

  [ ] フロントエンドから認証プロバイダーを呼び出し、実際のログイン・ログアウト処理が実行できること。
  [ ] ユーザーの認証状態（未ログイン / ログイン済）をアプリケーションのどこからでも参照できる状態になっていること。
  [ ] ログイン状態に応じて、ナビゲーションメニュー（「ダッシュボードへ」など）が正しく切り替わること。
  [ ] APIへの通信時に、認証トークンを自動的に付与するリクエスト制御の仕組みが設定されていること。

  ### 備考・参考リンク

  セキュリティ上の理由から、トークンの有効期限切れに伴うセッションのリフレッシュ処理も考慮した設計にしてください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/15

------------------------------------------

[Task]: HonoにおけるJWT検証ミドルウェアと、各種データのCRUDロジックの実装 otonasi-muonn/megalo2026#14
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  Epic 1で作成したバックエンドの空ルーティングに対して、実際の中身を実装します。

  認可が必要なエンドポイント（ステージ作成、プロフィール更新など）を保護するためのJWT検証ミドルウェアを導入します。さらに、フロントエンドからのリクエスト内容（JSON）を解析し、Supabaseデータベースに対してデータの作成（Create）、読み取り（Read）、更新（Update）、削除（Delete）を行う処理を構築します。

  ### 完了の定義（Doneの条件）

  [ ] HTTPヘッダーの  Authorization: Bearer <token> 
  からJWTを抽出し、正当性を検証するミドルウェアが実装されていること。
  [ ] トークンがない、または無効な場合は  401 Unauthorized  を返すこと。
  [ ] ステージの作成・更新・削除において、操作実行者（JWTから取得したID）と作成者（ author_id 
  ）が一致しているかを検証する認可ロジックが実装されていること。
  [ ] プレイログの送信APIにおいて、未認証ユーザーの場合は  player_id  を null として記録できること。
  [ ]  POST /api/stages/:id/likes （トグル）と  GET /api/profiles/me/likes 
  （取得）が実装され、重複評価防止の前提で整合していること。
  [ ]  POST /api/stages/:id/play_logs  実行時に、 stages.play_count  /  stages.clear_count 
  の統計更新が反映されること。

  ### 備考・参考リンク

  他人のステージを勝手に書き換えられないよう、更新・削除系のエンドポイントでは特に厳密な権限チェックを行ってください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/14

------------------------------------------

[Task]: 認証イベントを検知するDBトリガーおよびプロフィール自動生成機能の実装 otonasi-muonn/megalo2026#13
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  API層を介さずに、ユーザーアカウントの作成と同時にプロフィールデータを確実に生成するため、データベース側の機能（PostgreSQLの関数とトリガー）を利用します。

  Supabase Authでの新規ユーザー登録（ auth.users  へのINSERT）をフックし、同一トランザクション内で  profiles 
  テーブルに初期レコードを自動生成するロジックを実装します。

  ### 完了の定義（Doneの条件）

  [ ] 認証ユーザー作成イベント（ on_auth_user_created 等）に反応するデータベーストリガーが作成されていること。
  [ ] Google OAuth等から取得したメタデータ（ raw_user_meta_data->>name ）を抽出し、 profiles.display_name 
  として保存する関数が実装されていること。
  [ ] テスト用のアカウントを作成した際、 profiles  テーブルに自動的にデータが追加されることが確認できていること。

  ### 備考・参考リンク

  API設計仕様書の「イベント駆動処理」の項目を参照してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/13

------------------------------------------

[Task]: データベースのテーブル群（プロフィール、ステージ、プレイ履歴、いいね）のスキーマ構築 otonasi-muonn/megalo2026#12
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  データベース設計仕様書に基づき、Supabase（PostgreSQL）上にアプリケーションの基盤となる4つの主要テーブル（ profiles ,
  stages ,  play_logs ,  likes ）を作成します。

  各カラムのデータ型、デフォルト値、主キー（複合主キー含む）、および外部キー制約（ ON DELETE CASCADE 
  等）を正確に定義し、マイグレーションファイルとして管理します。

  ### 完了の定義（Doneの条件）

  [ ]  profiles  テーブルがSupabase Auth（ auth.users ）のIDを外部キーとして作成されていること。
  [ ]  stages  テーブルが作成され、JSONB型の  stage_data  カラムや統計用カラムが定義されていること。
  [ ]  play_logs  テーブルおよび、重複評価を防ぐ複合主キーを持った  likes  テーブルが作成されていること。
  [ ] すべてのスキーマ定義がマイグレーションSQLとしてリポジトリにコミットされていること。

  ### 備考・参考リンク

  Row Level Security (RLS)
  の設定については、今回はHono（APIサーバー）経由でのアクセスを前提とするため、サービスロールキーを利用する場合はバイパスされる点に留意してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/12

------------------------------------------

[Task]: フロントエンドコンポーネント内へのKAPLAYキャンバスの配置と初期化処理 otonasi-muonn/megalo2026#11
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  Reactのレンダリングサイクルから独立してKAPLAYエンジンを制御するため、初期化とマウントを担うカスタムフック（ 
  useKAPLAY ）の骨格を実装します。

  ステージ制作画面およびプレイ画面にCanvas要素を配置し、KAPLAYを起動してテスト用の図形を描画する状態を構築します。

  ### 完了の定義（Doneの条件）

  [ ]  useKAPLAY フックを用いて、指定したCanvas要素にKAPLAYインスタンスが正常にマウントされること。
  [ ] コンポーネントのアンマウント時にKAPLAYインスタンスが適切に破棄され、メモリリークを防ぐ処理が実装されていること。
  [ ] 起動したキャンバス上に、テスト用の図形（固定オブジェクト）がエラーなく描画されること。

  ### 備考・参考リンク

  フロントエンド・ゲーム設計仕様書の「KAPLAY制御フックとデータ共有」の設計指針を遵守してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/11

------------------------------------------

[Task]: フロントエンドにおける全画面のルーティングとAPI通信のボイラープレート実装 otonasi-muonn/megalo2026#10
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  Reactアプリケーションにおいて、全画面（ホーム、ダッシュボード、ステージ制作、プレイ、リザルト）のルーティングを設定します。

  各画面コンポーネント内にTask
  1で構築したバックエンドAPIへの非同期通信処理（データフェッチ）を記述し、取得したモックデータを画面やコンソールに出力してエンドツーエンドの疎通確認を行います。

  ### 完了の定義（Doneの条件）

  [ ] 定義されたすべてのURLパスで、対応する画面コンポーネントがエラーなく描画されること。
  [ ] コンポーネントのマウント時や特定のアクション時にAPI通信が行われ、バックエンドからモックデータを取得できること。
  [ ] 取得したデータをStateとして保持し、UI層へ受け渡す準備が整っていること。

  ### 備考・参考リンク

  フロントエンド設計仕様書の「ルーティングと画面遷移フロー」を参照してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/10

------------------------------------------

[Task]: バックエンドにおける全APIエンドポイントの空ルーティングとモックデータ返却の実装 otonasi-muonn/megalo2026#9
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  Honoを用いて、API設計仕様書に定義されたすべてのRESTfulエンドポイント（ /api/profiles/me ,  /api/stages 
  等）のルーティングを構築します。

  データベース接続やJWT認証等のロジックは省略し、Epic
  0で合意した型定義に基づく固定のJSONデータ（モックデータ）をHTTPステータス200で返却する処理のみを実装します。

  ### 完了の定義（Doneの条件）

  [ ] 設計書に記載された全エンドポイント（GET, POST, PUT, DELETE）へのルーティングが定義されていること。
  [ ] 各エンドポイントへのリクエストに対し、エラーを吐かずにモックデータが正常に返却されること。
  [ ]  /api/stages  のフィルタ・ページネーション用クエリパラメータを含むモックレスポンスの疎通確認ができること。
  [ ] CORS設定が適切に行われ、ローカルのフロントエンド環境からのリクエストを受信・処理できること。

  ### 備考・参考リンク

  API設計仕様書（ 01_api-design.md ）のエンドポイント一覧を参照してください。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/9

------------------------------------------

[Task]: Supabase CLIを用いたローカル開発用エミュレーター環境の構築 otonasi-muonn/megalo2026#8
Open • otonasi-muonn opened about 8 hours ago • 0 comments
Labels: task


  ### タスクの概要

  各開発者が他者の作業（テーブル定義の変更など）に影響されることなく、自身のローカルPC上で独立してデータベースおよび認証基盤を立ち上げられるよう、Supabase
  CLIを導入します。同時に、開発初期の動作確認に必要となるダミーデータ（テストユーザー等）を自動投入するためのシードスクリプトも作成します。

  ### 完了の定義（Doneの条件）

  [ ] リポジトリ内にSupabase CLIの初期化設定が完了していること。
  [ ] ローカル環境で起動コマンドを実行し、コンテナ上でデータベースおよび認証基盤が正常に立ち上がること。
  [ ] シードファイルを用いて、ローカルDB起動時に開発用の初期データが自動投入されること。
  [ ] README等に、新規参画メンバー向けの「ローカル環境立ち上げ手順」が明確に追記されていること。

  ### 備考・参考リンク

  エミュレーターの実行にはコンテナ環境（Docker等）が必須となるため、事前に各開発メンバーのPC環境を確認しておくことを推奨します。

  --------


View this issue on GitHub: https://github.com/otonasi-muonn/megalo2026/issues/8

------------------------------------------

[Task]: 共有パッケージにおけるステージデータの型定義とドメインモデルの設計 otonasi-muonn/megalo2026#7
Open • otonasi-muonn opened about 8 hours ago • 1 comment
Labels: task


  ### タスクの概要

  フロントエンド（UI・ゲームエンジン）とバックエンド（API・DB）間で通信する「ステージデータ」のJSON構造を正確に設計し、共有ディレクトリ配下に型定義として実装します。多種多様なギミックの座標・種類・固有パラメーターを、柔軟かつ安全に表現できるデータ構造を定義し、チーム全体で仕様の合意形成を行うことが目的です。

  ### 完了の定義（Doneの条件）

  [ ] 共有ディレクトリ内に、ステージデータに関する型定義ファイルが作成されていること。
  [ ] UI、ゲームロジック、データベースの各担当者間で、データ構造（JSONの持ち方）の合意が取れていること。
  [ ] フロントエンドおよびバックエンドの各プロジェクトから共有の型定義をインポートし、正常にビルドが通ること。

  ### 備考・参考リンク

  データベース設計仕様書におけるステージデータ格納用カラム（JSONB型など）の構造と、フロントエンドで扱う型定義を完全に一致させる必要があります。

  --------
